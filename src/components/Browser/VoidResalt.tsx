import styles from "./Browser.module.scss";

function VoidResult() {
  return (
    <section className={styles.voidResalt}>
      <h2 className={styles.voidResalt__title}>This folder has no files</h2>
    </section>
  );
}

export default VoidResult;
